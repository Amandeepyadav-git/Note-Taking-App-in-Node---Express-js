<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
        <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
<div class="main w-full min-h-screen bg-zinc-900">
    <div class="form p-10 text-white">
        <form action="/create" method="post">
            <input class="block px-5 py-2 bg-zinc-800 w-full outline-none" type="text" placeholder="title goes here" name="title">
            <textarea class="block px-5 py-2 bg-zinc-800 w-full outline-none resize-none mt-2" name="details" placeholder="write your task details here" id="" ></textarea>
            <input class="mt-2 bg-blue-600 px-5 py-2" type="submit" value="create task">
        </form>
    </div>
    <div class="tasks p-10 flex gap-3 flex-wrap">
       <% if(files.length>0){%> 
        <% files.forEach(function(val){ %>
               <div class="task w-72 px-3 py-4 bg-zinc-800">
            <h1 class="text-white text-2xl tracking-tighter"><%= val %></h1>
             <a href="/files/<%= val%>" class="text-blue-400 inline-block mt-3">ReadMore</a>
        </div>
       
            <%})} else{%>
 <h3 class="text-zinc-600">No task saved yet</h3>
                <% } %>   
    </div>
</div>
</body>
</html>